---
apiVersion: v1
kind: Service
metadata:
  name: rollout-canary-preview
  labels:
    app: summer-k8s-app  
spec:
  type: NodePort
  selector:
    app: summer-k8s-app
  ports:
      - nodePort: 32000
        protocol: TCP
        port: 8080

        